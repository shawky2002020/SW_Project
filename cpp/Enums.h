#ifndef ENUMS_H
#define ENUMS_H

// Corresponds to the <<enumeration>> TransactionTyp in the diagram
enum class TransactionType {
    DEPOSIT,
    WITHDRAWAL,
    TRANSFER,
    BILL_PAYMENT
};

#endif // ENUMS_H
